<Window x:Class="RUAIUnlocker.AddEntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить запись в hosts" Height="260" Width="440"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#0F172A" Foreground="#F8FAFC">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Добавить запись в hosts" FontSize="16" FontWeight="Bold" Foreground="#F8FAFC" Margin="0,0,0,16"/>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="IP-адрес:" VerticalAlignment="Center" Foreground="#94A3B8" FontSize="13"/>
            <TextBox x:Name="TxtIp" Grid.Column="1" Text="127.0.0.1" Background="#1E293B" Foreground="#F8FAFC" BorderBrush="#334155" Padding="8,6" FontSize="13"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Домен:" VerticalAlignment="Center" Foreground="#94A3B8" FontSize="13"/>
            <TextBox x:Name="TxtDomain" Grid.Column="1" Background="#1E293B" Foreground="#F8FAFC" BorderBrush="#334155" Padding="8,6" FontSize="13"/>
        </Grid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Click="BtnCancel_Click" Margin="0,0,8,0" Background="#1E293B" Foreground="#F8FAFC" BorderBrush="#334155" Padding="16,8" Cursor="Hand"/>
            <Button Content="Добавить" Click="BtnAdd_Click" Background="#8B5CF6" Foreground="#FFFFFF" BorderBrush="#8B5CF6" FontWeight="SemiBold" Padding="16,8" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
